# ── Server ──────────────────────────────────────────────────
# Module B runs on 5000. After merge with Module A (port 8080),
# the combined app will use 8080. Change this line only at merge time.
server.port=5000

# ── MongoDB ──────────────────────────────────────────────────
spring.data.mongodb.uri=mongodb+srv://admin:root@unilife.fkbmznz.mongodb.net/lecture_booking?retryWrites=true&w=majority&appName=unilife

# ── Logging ──────────────────────────────────────────────────
logging.level.com.smartcampus=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# ── MERGE NOTE ───────────────────────────────────────────────
# When merging with Module A (facilities), the following will be added
# by Module A's application.properties. Do NOT duplicate them:
#   spring.application.name=smart-campus
#   logging.level.org.springframework.security=DEBUG
