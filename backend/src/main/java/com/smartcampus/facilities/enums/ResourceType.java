package com.smartcampus.facilities.enums;

/**
 * Represents the type/category of a bookable campus resource.
 * Used in search/filter operations and displayed in the UI.
 */
public enum ResourceType {
    LECTURE_HALL,
    LAB,
    MEETING_ROOM,
    PROJECTOR,
    CAMERA,
    OTHER
}
